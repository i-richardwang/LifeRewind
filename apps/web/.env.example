# PostgreSQL connection string
DATABASE_URL=postgresql://user:password@localhost:5432/liferewind

# Collector API authentication key (must match the key configured in collector)
COLLECTOR_API_KEY=your-secret-api-key
